<h1>Home</h1>

<% @posts.each do |post| %>
  <%= post.title %>
  <%= post.content %>
  <%= link_to "comment?", new_user_post_comment_path(post.user_id, post.id) %><br>
<% end %>


new_user_post_comment_path

